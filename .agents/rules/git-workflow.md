# Git Auto-Push Rule

After implementing any requested changes, fixes, or additions:
1. Always stage the modified/created files (`git add`).
2. Commit the changes using a descriptive Conventional Commit message (e.g., `feat:`, `fix:`, `style:`, `refactor:`).
3. Push the commits directly to GitHub (`git push origin <branch>`).
